@IsTest
private class MyNewClass_Test {
    @IsTest
    static void coversMyNewClass() {
        // 🔹 Call at least one real method on MyNewClass so lines get covered.
        // If MyNewClass already has a method, call it here.
        // Example 1 (static):
        //   System.assertEquals('pong', MyNewClass.ping());

        // Example 2 (instance):
        //   MyNewClass obj = new MyNewClass();
        //   System.assertNotEquals(null, obj.someMethod());

        // Temporary safety so the test compiles even before you edit the line above:
        System.assert(true);
    }
}
