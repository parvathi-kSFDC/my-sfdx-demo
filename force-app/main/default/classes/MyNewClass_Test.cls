@IsTest
private class MyNewClass_Test {
    @IsTest
    static void coversMyNewClass() {
        // Adjust this to call a real method from MyNewClass.
        // Example based on your latest edit:
        System.assertEquals('pong', MyNewClass.ping());
    }
}
